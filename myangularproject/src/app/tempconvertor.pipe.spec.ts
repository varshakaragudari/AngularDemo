import { TempconvertorPipe } from './tempconvertor.pipe';

xdescribe('TempconvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new TempconvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
